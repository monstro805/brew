name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ Mandolin13 }} event."
      - run: echo "🐧 This job is now running on a ${{ mando805ox }} server hosted by armando805ox36186!"
      - run: echo "🔎 The name of your branch is ${{ Monsters-brew }} and your repository is ${{ Monsters-brew.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ Monsters-brew.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ brew.sh.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."

